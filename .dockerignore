.git
.idea
node_modules
vendor
storage/logs/*
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*

# Frontend build artifacts — frontend has its own Dockerfile + context
frontend/node_modules
frontend/dist

# Jenkins stack lives in this repo but must NOT enter the app image build
# context (it contains root-owned certbot data that breaks `docker build`).
jenkins

# Local env files — image gets env at runtime
.env
.env.docker
.env.production
